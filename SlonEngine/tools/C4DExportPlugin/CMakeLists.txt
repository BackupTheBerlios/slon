SET (C4D_EXPORT_PLUGIN_OUTPUT_DIRECTORY ${CFG_RUNTIME_OUTPUT_DIRECTORY}/tools/C4DExportPlugin)

ADD_CUSTOM_TARGET(C4DExportPlugin)
SET_TARGET_PROPERTIES (C4DExportPlugin PROPERTIES
    FOLDER ${TOOLS_PROJECT_GROUP}
)

IF (MSVC)
    SET (BINGINDS_PATH ${RUNTIME_OUTPUT_DIRECTORY}/${CMAKE_CFG_INTDIR})
ELSE (MSVC)
    SET (BINGINDS_PATH ${RUNTIME_OUTPUT_DIRECTORY})
ENDIF (MSVC)
ADD_DEPENDENCIES(C4DExportPlugin PythonBindings)

ADD_CUSTOM_COMMAND(TARGET  C4DExportPlugin
                   COMMAND ${CMAKE_COMMAND} -E copy_if_different "${CMAKE_CURRENT_SOURCE_DIR}/SlonEngineExport.pyp" "${C4D_EXPORT_PLUGIN_OUTPUT_DIRECTORY}/SlonEngineExport.pyp"
                   COMMAND ${CMAKE_COMMAND} -E copy_directory "${CMAKE_CURRENT_SOURCE_DIR}/res" "${C4D_EXPORT_PLUGIN_OUTPUT_DIRECTORY}/res"
                   COMMAND ${CMAKE_COMMAND} -E copy_directory "${CFG_RUNTIME_OUTPUT_DIRECTORY}/extensions/PythonBindings/slon" "${C4D_EXPORT_PLUGIN_OUTPUT_DIRECTORY}/slon")
         
